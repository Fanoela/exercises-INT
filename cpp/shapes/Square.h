class Square{

    private:
        float side;
    public:
        float perimeter ();
        float area ();
        void changeSide(float new_side);
        
        Square(); //constructeur
        ~Square(); //destructeur


};